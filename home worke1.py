# list
append, insert, extend, remove, count, revers, clear
# методы
Экранированые последовательности, неформатированые строки, сложение строк, длинна строки,
поиск подстрок, замена шаблона
# dict
dict.fromkeys, dict.get, dict.pop, dict.update, dict.values
# set
if len(set.isdisjoint, len(s), x in s, set.union, set.intersection,
       set.difference, set.symmetric_difference:
    